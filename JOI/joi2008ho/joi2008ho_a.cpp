#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

signed main(void)
{
	int n;
	cin >> n;
	int index, pre;
	int stone[n], tab[n];
	rep(i, n) cin >> stone[i];
	intdex = 0;
	tab[index] = 1;
	pre = stone[0];
	for(int i = 1; i < n; i++)
	{
		if (pre == stone[i])
		tab[index]++;
		else if ()
		{
			if ()
			else
		}
		else
		{
		}
	}
	return 0;
}
