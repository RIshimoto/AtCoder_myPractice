#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, d;
	cin >> n >> d;
	int a[n];
	rep(i, n) cin >> a[i];
	rep(i, n)
	{
		for(int j = n - 1; j >= i; j--)
		{
			if (a[j] - a[i] <= d)
				ans += (j - i) * 

		}
	}
}
