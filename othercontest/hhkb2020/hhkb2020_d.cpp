#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i <(n); i++)
using namespace std;
typedef long long ll;
using P=pair<ll, ll>;

const int mod = 1e9+7;
signed main(void)
{
	int t;
	cin >> t;
	rep(i, t)
	{
		int n, a, b;
		cin >> n >> a >> b;
	}
	return (0);
}
