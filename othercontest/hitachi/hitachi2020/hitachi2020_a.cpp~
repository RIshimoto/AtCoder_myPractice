#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i <= (n); i+=2)
using namespace std;

signed main(void)
{
	string s;
	cin >> s;
	rep(i, s.size() / 2)
	{
		if (s.substr(i, 2) != "hi")
		{
			cout << "No" << endl;	
			return 0;
		}
	}
	cout << "Yes" << endl;
	return 0;
}
