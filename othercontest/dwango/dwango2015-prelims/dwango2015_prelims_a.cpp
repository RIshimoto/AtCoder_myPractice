#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, x;
	cin >> n >> x;
	cout << 540 * x + 525 * (n - x) << endl;
	return 0;
}
