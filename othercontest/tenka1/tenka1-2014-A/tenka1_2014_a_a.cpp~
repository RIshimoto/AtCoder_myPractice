#include <bits/stdc++.h>
#define rep(i, n) for(int i = 1; i <= (n); i++)
using namespace std;

signed main(void)
{
	vector<string> ans(1000);
	rep(i, 1000) ans[i - 1] = to_string(i);
	sort(ans.begin(), ans.end());
	for(int i = 0; i < 1000; i++)
		cout << ans[i] << endl;
	return 0;
}
