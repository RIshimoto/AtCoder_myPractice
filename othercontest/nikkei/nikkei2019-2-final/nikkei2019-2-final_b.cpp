#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	string s;
	cin >> s;
	for(int i = 1; i <= n - 4; i++)
	{
		for(int j = 0; i + j <= n - 4; j++)
		{
			for(int k = j + 1; k + i <= n - 3; k++)
			{
				if (s.substr(i, j) == s.substr(n - 5, 
			}
		}
	}
}
