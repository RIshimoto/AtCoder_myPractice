#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, m;
	cin >> n >> m;
	int a[m], b[m];
	rep(i, m) 
	{
		cin >> a[i] >> b[i];
		if (b[i] == m)
		
	}
}
