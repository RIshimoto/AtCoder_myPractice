#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, k;
	cin >> n >> k;
	n /= 2;
	if (n % 2 != 0)
		n++;
	if (n >= k)
	cout << "YES" << endl;
	else
	cout << "NO" << endl;
	return 0;
}
