#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

const int A = 1e6;
signed main(void)
{
	int n, m;
	cin >> n >> m;
	int r[m];
	rep(i, m) cin >> r[i];
	r[i] * 100
	cout << fixed << setprecision(20) << ans << endl;
	return 0;
}
