#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

signed main(void)
{
	int n, m, ans;
	cin >> n >> m;
	ans = n / (m + 1);
	cout << ans << endl;
	return 0;
}

