#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, m;
	cin >> n >> m;
	if (n > m)
	{
		int a = n / m;
		if (n % m != 0) a++;
		cout << a * m - n << endl;
	}
	else
		cout << m - n << endl;
	return 0;
}
