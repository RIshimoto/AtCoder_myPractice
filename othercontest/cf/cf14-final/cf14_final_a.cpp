#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int s;
	double ans;
	cin >> s;
	ans = (double) 50 / s;
	cout << fixed << setprecision(20) << ans << endl;
	return 0;
}
