#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int r, d;
	cin >> r >> d;
	double ans = r * r * 3.14159265 * 2 * d * 3.14159265;
	cout << fixed << setprecision(20) << ans << endl;
	return 0;
}
