#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	string s;
	cin >> s;
	if (s.substr(0, 5) == "MUJIN")
		cout << "Yes" << endl;
	else
		cout << "No" << endl;
	return 0;
}
