#include <bits/stdc++.h>
using namespace std;

signed main(void)
{
	int n;
	cin >> n;
	for(int i = 0; i < n; i++)
	{
		int a, b;
		cin >> a >> b;
		cout << (long long)a * b << endl;
	}
	return 0;
}
