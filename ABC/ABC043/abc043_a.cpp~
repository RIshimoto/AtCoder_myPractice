#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, ans;
	cin >> n;
	ans = 0;
	for (int i = n; i > 0; i--)
		ans += n;
	cout << ans << endl;
	return 0;
}
