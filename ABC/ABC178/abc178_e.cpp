#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;
using P=pair<int, int>;

signed main(void)
{
	int n;
	ll ans;

	cin >> n;
	rep(i, n) 
	{
		ll x, y;
		cin >> x >> y;
	}
	cout << ans << endl;
	return 0;
}
