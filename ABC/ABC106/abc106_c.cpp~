#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	string s;
	int k;
	cin >> s >> k;
	if (k < s.size() && s[k - 1] == '1')
		cout << 1 << endl;
	else
	{
		rep(i, s.size())
		{
			if (s[i] != '1')
			{
				cout << s[i] << endl;
				return 0;
			}
		}
		cout << 1 << endl;
	}
	return 0;
}
