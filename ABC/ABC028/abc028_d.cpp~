#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, k;
	long long count;
	double ans;
	
	cin >> n >> k;
	count = 0;
	count += (n - k) * (k - 1) * 6 + (n - 1) * 3 + 1;
	ans = count / pow(n, 3);
	cout << fixed << setprecision(15) << ans << endl;
	return 0;
}
