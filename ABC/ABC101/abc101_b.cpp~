#include <bits/stdc++.h>
using namespace std;

signed main(void)
{
	int n;
	cin >> n;
	int sum, tmp;
	sum = 0;
	tmp = n;
	while(n)
	{
		sum += n % 10;
		n /= 10;
	}
	if (tmp % sum == 0)
		cout << "Yes" << endl;
	else
		cout << "No" << endl;
	return 0;
}
