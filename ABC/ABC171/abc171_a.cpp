#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n);  i++)
using namespace std;
typedef long long ll;
using P = pair<int, int>;

signed main(void)
{
	char a;
	cin >> a;
	if (a >= 'A' && a <= 'Z')
		cout << 'A' << endl;
	else
		cout << 'a' << endl;
	return 0;
}
