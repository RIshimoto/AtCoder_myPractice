#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

signed main(void)
{
	int n, m;
	cin >> n >> m;
	string s[m];
	int c[m];
	rep(i, m) cin >> s[i] >> c[i];

	return 0;
}
