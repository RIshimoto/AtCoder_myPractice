#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int ans;
	ans = 0;
	rep(i, 7)
	{
		int a, b;
		cin >> a >> b;
		ans += max(a, b);
	}
	cout << ans << endl;
	return 0;
}
